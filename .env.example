# Google Maps JavaScript API key, restricted by HTTP referrer in Google
# Cloud Console (Maps JavaScript API + Distance Matrix API enabled).
#
# Optional: if unset, or if the key/APIs fail to load at runtime, the app
# falls back to Leaflet + OpenStreetMap automatically (map rendering and
# hotel-to-restaurant distances alike).
VITE_GOOGLE_MAPS_API_KEY=
